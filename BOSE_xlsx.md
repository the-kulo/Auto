graph TD;
    A[读取Excel文件] --> B[筛选Severity列中值为4或5的行]
    B --> C[将筛选结果写入新的Excel文件]
    C --> D[使用openpyxl加载工作簿]
    D --> E[创建Sheet2和Sheet3工作表]
    E --> F[设置需要保留的列]
    F --> G[重命名列的字典]
    G --> H[写入列标题到Sheet2和Sheet3的第一行]
    H --> I[添加三列分别为环境，修复途径，备注]
    I --> J[筛选DNS列中包含'-d'和'-p'的行]
    J --> K[提取环境列数据]
    K --> L[根据Results列中的关键词提取修复途径和备注]
    L --> M[添加修复途径和备注列]
    M --> N[将筛选后的数据写入Sheet2和Sheet3]
    N --> O[保存工作簿]
    O --> P[打印完成消息]
